#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
#include"swap.h"
int main()
{
	//交换数
	//int a = 0;
	//int b = 0;
	//int* pa = &a;
	//int* pb = &b;
	//scanf("a=%d,b=%d", &a, &b);
	//Swap(&a, &b);//传址调用
	//printf("a=%d,b=%d\n", a, b);
	//return 0;
	//字符转ASCII码值
	/*char arr = 0;
	scanf("%c", &arr);
	printf("%d", arr);
	return 0;*/
	//f(x)函数
	/*int x = 0;
	int y = 0;
	while (1)
	{
		scanf("%d", &x);
		if (x < 0)
			y = 1;
		else if (x == 0)
			y = 0;
		else
			y = -1;
		printf("%d\n", y);
		if (getchar() != EOF) continue;
		else break;
	}
	return 0;*/
}
